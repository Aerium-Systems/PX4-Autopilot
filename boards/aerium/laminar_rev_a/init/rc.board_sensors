#!/bin/sh
#
# board sensors init
#
# Only sensor populated on this board: MS4525DO differential-pressure (airspeed)
# on the external I2C bus. Published to CAN via uavcannode (RAW_AIR_DATA).
#------------------------------------------------------------------------------

if param compare -s SENS_EN_MS4525DO 1
then
	ms4525do start -X
fi
